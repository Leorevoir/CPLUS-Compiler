/* add takes two integers & returns their sum */
def add(a: int, b: int) -> int
{
    return a + b;
}

/* main is the entry point of every C+ program */
def main() -> int
{
    /* result is dynamically set to the return value of add -> int */
    result = add(1, 2);
    return result;
}
